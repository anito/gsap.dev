<IfModule mod_rewrite.c>
	  RewriteEngine on
	  RewriteBase /
	  RewriteCond %{ENV:REDIRECT_STATUS} 200
	  RewriteRule ^/(.*) []
	  #RewriteRule ^(.*) https://gap.webpremiere.dev [L,R]
</IfModule>
<IfModule mod_deflate.c>
	AddType image/svg+xml .svg .svgz
</IfModule>